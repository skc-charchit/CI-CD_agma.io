# Company Web

The company website is the public catalog and entry point for all products.

- `src/`: Website source code.
- `public/`: Static assets.

Each product should have its own detail page and a clear link to the product
application. This application is frontend-only; shared server-side behavior
belongs in `apps/api`.