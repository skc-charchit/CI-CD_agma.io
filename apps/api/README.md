# Platform API

The Python application lives in `app/`. This service owns shared platform
capabilities and APIs used by the company website and products. Browser-facing
code does not belong here; it belongs in `apps/company-web` or a product's
`frontend/` directory.