# Product Frontend

## Owns

The browser application for a product created from this template. Its public
entry files live in `public/` and its source code lives in `src/`.

## Do not put here

Company website code, shared platform API code, secrets, or Kubernetes
manifests.

## Stop here

Copy this frontend into the product repository before implementing product
features. Shared UI belongs in `packages/ui`.
