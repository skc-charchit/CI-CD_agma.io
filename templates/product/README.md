# Product Template

Copy this directory into a new product repository. Keep browser code in
`frontend/` and server code in `backend/`. Consume reusable packages from the
platform repository and keep product deployment manifests in
`infra/kubernetes/products/<product-name>/`.