# Infrastructure

Store deployment, hosting, containers, and environment configuration here.

- `kubernetes/`: Kubernetes manifests organized with Kustomize.
- `terraform/`: Cloud infrastructure, networking, databases, and managed services.
- `helm/`: Helm charts only when packaging or distributing a reusable chart is needed.